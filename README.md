# prac15backend

Simple Node.js and Express backend for the `prac15` React frontend.

## Routes

- `GET /message` returns a welcome message in JSON.
- `POST /submit` accepts `name` and `email` as JSON and returns a confirmation.

## Run

```bash
npm install
npm start
```

The server runs on `http://localhost:5050` by default.
